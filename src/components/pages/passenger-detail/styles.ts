import styled from "styled-components";

export const TitleStyled = styled.h1`
    color: #738290;
    font-size: 32px;
    font-weight: 500;
    position: absolute;
    top: 60px;
    font-weight: 600;
`
